namespace TapRunner.Domain
{
    /// <summary>Chapter 11 sample — high-level run state for Tap Runner.</summary>
    public enum TapRunnerGamePhase
    {
        Ready = 0,
        Running = 1,
        GameOver = 2
    }
}
